# ==================== 开发环境 ====================
# 本地开发用，配置文件里直接写死，不需要 IDEA 环境变量

spring:
  datasource:
    url: jdbc:mysql://172.30.0.4:3306/pet?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: mysql
    password: "!Jirong199410"
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 172.30.0.4
      port: 6379
      database: 10
      password: "Jirong199410!"
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 2
          max-wait: 1000
        shutdown-timeout: 2000

# ==================== AI 配置（开发环境可以写死测试 key） ====================
ai:
  dashscope:
    api-key: ${DASHSCOPE_API_KEY:sk-96280128c4384cdb9e5753fdcc458773}
    base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
    model: qwen3.5-flash
