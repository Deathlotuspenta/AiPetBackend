# ==================== 开发环境 ====================
# 本地开发用，配置文件里直接写死，不需要 IDEA 环境变量

spring:
  datasource:
    url: jdbc:mysql://cq-cdb-26vwu1ld.sql.tencentcdb.com:22843/pet?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: "root"
    password: "Lcw20031010"
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 172.30.0.4
      port: 6379
      database: 10
      password: "Jirong199410!"
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 2
          max-wait: 1000
        shutdown-timeout: 2000

# ==================== AI 配置（开发环境可以写死测试 key） ====================
ai:
  dashscope:
    api-key: "sk-310945a6bbe148c1ae890a465bfd9572"
    base-url: https://api.deepseek.com
    model: deepseek-v4-flash
